import { useCreateSubscriber as useGeneratedCreateSubscriber } from "@workspace/api-client-react";

/**
 * Re-exporting the generated hook to maintain project structure conventions.
 * The generated hook already includes full types and TanStack Query integration.
 */
export function useCreateSubscriber() {
  return useGeneratedCreateSubscriber();
}
